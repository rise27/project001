name: Deploy Website

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Deploying to surge
    steps:
      - uses: actions/checkout@v2
      - name: Run surge
        run: surge ./ project001_2021.surge.sh --token ${{ secrets.SURGE_TOKEN }}
